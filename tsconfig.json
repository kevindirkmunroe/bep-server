{
    "compilerOptions": {
        /* Language and environment */
        "target": "ES2020",
        "module": "CommonJS",
        "lib": [
            "ES2020",
            "dom"
        ],

        /* Project structure */
        "rootDir": "src",
        "outDir": "dist",

        /* Module resolution */
        "moduleResolution": "node",
        "esModuleInterop": true,
        "resolveJsonModule": true,

        /* Handle CommonJS better */
        "allowSyntheticDefaultImports": true,

        /* Strictness (important signal) */
        "strict": true,
        "skipLibCheck": true,

        /* DX improvements */
        "forceConsistentCasingInFileNames": true,

        /* Source maps for debugging */
        "sourceMap": true,

        /* Allow running with ts-node easily */
        "types": ["node"],

        "ignoreDeprecations": "6.0",
      "jsx": "react"
    },

    "include": [
        "src",
        "types",
        "src/**/*",
        "ingestion/**/*",
        "scripts/**/*"
    ],
    "files": ["types/express-session.d.ts"],

    "exclude": [
        "node_modules",
        "dist"
    ]
}
